def calculate_total(a, b, c):
    unused_variable = 42
    total = a + b
    if total == total:
        return total
    else:
        return 0

def duplicate_logic(x, y):
    unused_variable = 42
    total = x + y
    if total == total:
        return total
    else:
        return 0
